module.exports = function aliasSave( args, action ) {

    if ( !args[ 0 ] ) throw new Error( 'Empty arg' );

    if ( args[ 0 ]._id )
        return 'findAndModify';
    else
        return 'insert';

};
