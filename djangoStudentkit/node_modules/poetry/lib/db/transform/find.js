const MongoDB = require( 'mongodb' );

module.exports = ( args ) => {
    if ( !args ) args = [];
    if ( !args[ 0 ] ) args[ 0 ] = {};
    if ( typeof args[ 0 ] == 'string' ) {
        if ( args[ 0 ].length == 24 ) try {
            args[ 0 ] = MongoDB.ObjectId( args[ 0 ] );
        } catch ( e ) {}
        args[ 0 ] = {
            _id: args[ 0 ]
        };
    }
    return args;
};
