const dbCollection = require( './lib/db/collection' );

module.exports = new Proxy( {}, {

    get( _, collection ) {
        if ( typeof dbCollection == 'function' )
            return dbCollection( collection );
    },

    set() {}

} );
